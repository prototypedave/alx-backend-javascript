#!/usr/bin/node
//prints a given string in STDOUT

const displayMessage = function displayMessage(message){
    console.log(message);
}

// export variable message
module.exports = displayMessage;